pipeline {
    agent any

    stages {
        stage('Terraform Destroy') {
            steps {
                // Switch to the apply job's workspace
                ws('/var/lib/jenkins/workspace/Jenkins_vpc_dev') {
                    dir('terraform/dev') {
                        sh 'terraform init'
                        sh 'terraform destroy -auto-approve'
                    }
                }
            }
        }
    }
}